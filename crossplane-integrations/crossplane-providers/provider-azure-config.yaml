apiVersion: azure.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default-azure
  namespace: crossplane-system
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: azure-credentials
      key: creds
