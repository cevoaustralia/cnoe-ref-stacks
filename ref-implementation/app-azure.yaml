apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-azure
  namespace: argocd
  labels:
    env: dev
spec:
  project: default
  source:
    repoURL: cnoe://app-azure
    targetRevision: HEAD
    path: "."
  destination:
    server: "https://kubernetes.default.svc"
    namespace: azure
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true